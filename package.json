{
  "name": "ts-model-serializer",
  "version": "0.0.5",
  "description": "",
  "keywords": [],
  "main": "./dist/index.js",
  "typings": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "author": "Jonas <jonas.ostlund@outlook.com>",
  "repository": {
    "type": "git",
    "url": ""
  },
  "license": "MIT",
  "engines": {
    "node": ">=6.0.0"
  },
  "scripts": {
    "build": "rm -rf dist/ && tsc",
    "clean": "rm -rf ./coverage",
    "test": "mocha --reporter spec --compilers ts:ts-node/register 'test/**/*.spec.ts'",
    "test:prod": "istanbul cover _mocha --report lcovonly -- -R spec --compilers ts:ts-node/register 'test/**/*.spec.ts'",
    "report": "cat ./coverage/lcov.info | coveralls"
  },
  "devDependencies": {
    "@types/chai": "^4.1.2",
    "@types/mocha": "^2.2.48",
    "chai": "^4.1.2",
    "coveralls": "^3.0.0",
    "istanbul": "^0.4.5",
    "jscover": "^1.0.0",
    "mocha": "^5.0.1",
    "mocha-lcov-reporter": "^1.3.0",
    "ts-node": "^5.0.0",
    "typescript": "^2.6.2"
  }
}
